import React, {FC} from 'react';
import LoginForm from "../../widgets/loginForm/LoginForm";

const Login: FC = () => {
    return (
        <div>
            <LoginForm/>
        </div>
    );
};

export default Login;